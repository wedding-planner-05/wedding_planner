import React, { useEffect, useState } from 'react'
import axios from "axios";
function CaterPage() {
    const [data, setData] = useState([]);

    // useEffect(() => {
    //     axios.get("").then(result => { setData(result) }).catch(err => { console.log(err) });
    // }, [])

    return (
     <>
      <div className='box'>
        
      </div>
     </>
    )
}

export default CaterPage
